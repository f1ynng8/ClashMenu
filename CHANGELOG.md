## v0.0.33

### 🚀 优化改进

- 收窄设置窗口整体宽度，减少界面横向占用
- 缩短“场景配置文件管理”和“Clash 配置文件管理”入口按钮宽度，使配置区域布局更加紧凑

## v0.0.32

### ✨ 新增功能

- 设置窗口新增“场景配置文件管理”和“Clash 配置文件管理”入口，并提供独立的原生管理窗口
- 配置管理列表支持区分本地配置与订阅配置
- Clash 配置支持逐项删除；删除订阅配置时会同时删除实际 YAML 文件并解除对应订阅

### 🚀 优化改进

- 精简主菜单“配置文件”子菜单，仅保留 YAML 配置文件列表与当前选中状态
- 设置页“配置文件”分组改为两个管理入口，场景和 Clash 配置的导入、订阅及更新操作统一收纳到对应窗口
- 场景配置管理保持单文件覆盖模式：导入新订阅会替换旧订阅及本地配置，导入本地配置会覆盖当前文件并解除原订阅
- 场景配置仅在存在有效订阅来源时启用“更新订阅链接”操作
- Clash 配置订阅更新操作使用“更新全部订阅”，明确会更新所有已记录订阅源的配置文件
- 场景自动或手动切换启用时，禁用 Clash 配置管理操作，避免普通配置与场景运行配置产生混淆

### 🐞 修复问题

- 删除当前 Clash 配置时自动切换到剩余配置并重启核心；没有剩余配置时自动停止核心
- 修复配置删除或解除订阅后 UserDefaults 残留问题，同步维护订阅映射、当前选中项和上次成功配置路径
- 最后一个 Clash 订阅被解除后会移除对应 UserDefaults 映射键；导入本地场景配置时会清除远程订阅 URL
- 内置 `ClashMenu.yaml` 调整为仅首次初始化，用户主动删除后不再自动恢复

## v0.0.30

### ✨ 新增功能

- 新增“场景”能力，支持按 Wi-Fi SSID 自动切换，或在手动模式下指定场景，并根据场景自动启动/停止 Mihomo、切换系统代理状态
- 支持导入本地或远程场景配置文件，加载 YAML 场景定义并生成对应运行时配置
- 新增场景配置解析与合并逻辑，可基于基础配置叠加 `rules`、`rule-providers`、`proxy-providers`、`proxies`、`proxy-groups` 等覆盖项

### 🚀 优化改进

- 设置窗口与菜单栏状态菜单同步接入场景控制入口，并补充位置权限、Wi-Fi 检测和场景状态管理所需的应用状态与服务层实现
- 打包与依赖配置随场景功能一并更新，引入 `Yams` 用于 YAML 解析，并补充相关工程与脚本调整
- 更新设置与启动流程相关截图，覆盖 `config`、`mode`、`setup`、`start` 等界面变化；新增 `scene` 与 `power_consumption` 截图，保持文档演示与当前界面一致
- 场景配置新增 `system-dns` 字段，可按场景写入系统 DNS，未配置时恢复自动 DNS

### 🐞 修复问题

- 扩展特权 helper 与 XPC 接口，补齐场景 `system-dns` 的系统级 DNS 设置与恢复自动 DNS 能力

## v0.0.29

### ✨ 新增功能

- 设置窗口在“通用设置”和“高级操作”之间新增“代理设置”分组，提供“忽略这些主机/域的代理”可编辑配置
- 系统代理 helper 现在会同步写入并校验 macOS 代理例外列表（ExceptionsList）

### 🚀 优化改进

- “忽略这些主机/域的代理”改为多行滚动文本输入，窗口尺寸同步放宽，便于查看和编辑较长列表
- 默认代理例外列表更新为：`localhost,127.0.0.1,*.local,10/8,169.254/16,172.16/12,192.168/16,apple.com,*.apple.com`
- 本地打包版本已推进到 `0.0.29`

### 🐞 修复问题

- 修复特权 helper 的 launchd 注册方式，改用绝对路径 `ProgramArguments`，避免重启后因相对路径记录导致 `spawn failed / EX_CONFIG`
- 为应用退出阶段补充同步关闭系统代理的兜底逻辑，减少非菜单退出路径下残留系统代理的概率
- 修复系统代理 helper 的 XPC 接口，补齐 `bypassHosts` 数组参数的解码声明，避免新增代理例外列表参数后消息被 NSXPC 丢弃

### 📝 排查备注

- 如果更新后仍然看到旧的 `com.clashmenu.helper` XPC 解码错误，需要退出 ClashMenu 并执行 `sudo launchctl bootout system/com.clashmenu.helper`，再重新打开 `/Applications/ClashMenu.app` 让新 helper 生效

## v0.0.22

### ✨ 新增功能

- 在“通用设置”中新增“恢复确认延迟（秒）”，统一控制网络恢复和系统唤醒后的自动恢复确认等待时间，默认值为 3 秒

### 🚀 优化改进

- 菜单栏图标支持根据 Mihomo 运行中/已停止状态切换自定义品牌图标，并保持系统模板渲染与多显示器菜单栏的原生变暗效果
- 调整“关于”弹窗信息布局，按“ClashMenu 版本 / Mihomo 版本 / 空行 / Copyright”展示

## v0.1.5

### 🐞 修复问题

- 修复 macOS 13 Intel 平台下的兼容性问题，提升应用在旧版 Intel 设备上的启动与界面稳定性

<details>
<summary><strong> ✨ 新增功能 </strong></summary>

- 新增无内核版本的 ClashBar 安装包，支持按需分发不内置 Mihomo 内核的应用版本
- 支持在未内置核心组件时提供首次启动引导，方便用户手动安装和配置 Mihomo 内核

</details>

<details>
<summary><strong> 🚀 优化改进 </strong></summary>

- 优化未内置内核场景下的启动流程，缺少托管内核时将延后自动启动并提供更清晰的提示信息
- 调整启动失败与 TUN 相关错误提示文案，帮助用户更快定位和处理手动安装内核后的运行问题
- 优化打包与发布流程，适配新的安装包结构并同步更新相关资源与文档

</details>
